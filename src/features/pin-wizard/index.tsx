import { Board } from '@/shared/types';

type Props = {
  board: Board;
  pinIndex: number;
};

export const PinWizard = ({ board, pinIndex }: Props) => {
  console.log('board', board);
  console.log('pinIndex', pinIndex);

  return (
    <>
      {
        //
      }
    </>
  );
};
